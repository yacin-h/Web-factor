import type { Invoice } from "@/types/invoice";

export default function Classic({invoice}:{invoice: Invoice}){

    return <div className="w-[794px] min-h-[1123px] mx-auto bg-red-200">
a
    </div>
}